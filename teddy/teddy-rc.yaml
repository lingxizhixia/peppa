apiVersion: v1
kind: ReplicationController
metadata:
  name: teddy4
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: teddy4
    spec:
      containers:
      - name: teddy4
        image: lingxizhixia/teddy:1.0
        ports:
        - containerPort: 8010
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 30
          timeoutSeconds: 1